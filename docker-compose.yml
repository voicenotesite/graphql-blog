version: '3.8'
services:
  blog:
    build: .
    container_name: graphql-blog
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./data.db
      - SECRET_KEY=change-me-in-production
    volumes:
      - ./data:/app/data
